[project]
name = "test_firmware"
default = "test"

[output]
dir = "release"

[bootloaders.main]
file = "build/bt.hex"
base_addr = 0x0800_0000
app_offset = 0x8000

[targets.merge_test]
type = "merge"
description = "Test only"
bootloader = "main"
app_file = "build/app.hex"
output_format = "hex"
output_name = "test_merge"

[targets.pack_test_srec]
type = "pack"
description = "Test srec packing"
bootloader = "main"
app_file = "build/app.hex"
output_format = ""
